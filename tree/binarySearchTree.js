/**
 * 二叉查找树: 任意一个节点的值大于或等于其左节点小于或等于其右节点
 */
class BST {
  constructor (node = null) {
    this.root = node
  }

  insert (data) {
    if (this.root === null) {
      return this.root.data = data
    } else {
      let parent = this.root
      while (true) {
      }
    }
  }
}